# Loaded by `next build` (NODE_ENV=production). Prerendered pages bake this
# into canonical/og:url tags; the Worker runtime gets the same value from
# `vars` in wrangler.jsonc. Not a secret — committed on purpose (see
# .gitignore exception).
NEXT_PUBLIC_SITE_URL=https://lbjtaskforce.com
